# every var begins with bracket
(?<!\\)\$[^({]\w+

#disallow +o => catch grep
#set -o errexit
multiline
^[^#]*set\s+-o\s+errexit

#set -o nounset
multiline
^[^#]*set\s+-o\s+errexit

#set -o pipefail
multiline
^[^#]*set\s+-o\s+pipefail

# ash check if correct (no -e)
^#!\/bin\/sh\s*(?!-)

# grep all if starts:
if\s*\[.+\];\s*then
#for all check
#has correct spacing outside
if\s\[\s.+\s\];\sthen
#error if == found
if\s\[\s.+==.+\s\];\sthen
#check if at least one operator is in
-eq
-ne
-gt
-lt
-ge
-le
!=
=
# check if "if" only contains [ var op var/literal ]
